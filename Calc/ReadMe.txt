To compile:
- set "BOOST_HOME" env variable (e.g 'c:/boost_1_57_8')
- set "DISLIN_HOME" env variable (e.g 'c:/dislin')
DISLIN_HOME requires to have subfolders x64 and x86 (for 64bit and 32bit platforms).